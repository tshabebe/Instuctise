import { validateGoogleCallback } from '@/auth/google.provider';

export {
  /* @next-codemod-error `validateGoogleCallback` export is re-exported. Check if this component uses `params` or `searchParams`*/
  validateGoogleCallback as GET,
};
