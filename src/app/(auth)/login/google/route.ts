import { createGoogleAuthorizationURL } from '@/auth/google.provider';

export {
  /* @next-codemod-error `createGoogleAuthorizationURL` export is re-exported. Check if this component uses `params` or `searchParams`*/
  createGoogleAuthorizationURL as GET,
};
